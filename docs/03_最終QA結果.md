# 最終QA結果

実施日時: 2026-09-07 15:17 JST
判定: **FAIL**

> ユーザー指示は「最終QAがPASSした場合のみ、外部送信直前で一度だけ停止し、Human承認を求める」。
> QAがFAILのため、**送信承認依頼（Human Gate）は上げない。** これは指示どおりの挙動。

---

## QAチェックリスト

| # | 確認項目 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 前回メールを特定・復元できたか | **PASS** | Thread `1a074928c10097b6`、全文・メタデータ復元済 |
| 2 | 対象契約書を一意に特定できたか | **PASS** | `AIシステム業務委託契約書_確認用_v1.1.pdf` の1件のみ |
| 3 | 契約書の正本がDriveに存在するか | **FAIL** | Drive上に不在。送信済メールの添付が唯一の実体 |
| 4 | 署名用PDFの実体を取得できたか | **FAIL** | Gmail MCPに添付バイナリ取得手段なし。Googleトークンも無効 |
| 5 | PDF本文と契約条件の突合ができたか | **FAIL** | 実体未取得のため突合不能（#4に従属） |
| 6 | 相手方の宛先メールが確定しているか | **PASS** | `storeradar@storeradar.jp`（前回メール宛先と同一） |
| 7 | 相手方の氏名・法人格が確定しているか | **FAIL** | 「菊池様」のみ。Gmail/Drive全域を検索したが実名情報なし |
| 8 | 相手方の内容確認が完了しているか | **FAIL** | 9/6送信→9/7 15:17時点で返信なし。前回メールは「問題なければ正式な締結手続きを進める」と条件付け |
| 9 | 署名位置を指定できたか | **FAIL** | PDF未取得のためページ数・署名欄座標が不明（#4に従属） |
| 10 | GMOサインへ登録できたか | **FAIL** | 認証情報が本環境に存在せずログイン不能 |
| 11 | 契約条件（金額・期間・法的条件）を変更していないか | **PASS** | 一切変更していない。本レポートは既存条件の記載のみ |
| 12 | 契約開始日が妥当か | **要決裁** | 開始日 2026-09-07 = 本日。締結完了は最短でも本日以降 |
| 13 | GMOサインの送信枠残があるか | **未確認** | 管理画面へアクセス不能。現行プランは送信数上限あり |

集計：PASS 4 / FAIL 7 / 要決裁 1 / 未確認 1

---

## FAIL項目の分類

### A. 本セッションで自己解決不能（認証・権限の欠如）

| 項目 | 内容 |
|---|---|
| #10 | GMOサインのID/PW/APIキーが環境変数・リポジトリ・Drive・Gmailのいずれにも存在しない。GMOサイン用のMCPコネクタも未接続 |
| #4 | Gmail MCPが提供するのは `get_message` / `get_thread` のみで、添付はメタデータのみ返却。`CLOUDSDK_AUTH_ACCESS_TOKEN` は tokeninfo で `invalid_token` |

いずれも**契約条件を変更しない範囲の技術的エラーではなく、資格情報の不在**。
リトライ・回避実装での自己解決は不可能。

なお、GMOサインAPI（`api.gmosign.com`）へのネットワーク到達性自体は確認済み（HTTP 200）。
不足しているのは認証情報のみ。

### B. 人の判断が必要（要決裁）

| 項目 | 論点 | 選択肢 |
|---|---|---|
| #8 | 菊池様の確認返信を待たずに締結送信するか | (a) 返信を待つ ／ (b) 催促する ／ (c) 返信前に締結送信する |
| #12 | 開始日9/7が本日である扱い | (a) そのまま締結（遡及なし・実務上許容） ／ (b) 開始日を締結日以降に修正 ※**条件変更のため要決裁** |

**推奨：#8は(b)催促、#12は(a)そのまま。**
理由：#8は前回メール本文が「問題なければ正式な締結手続きを進める」と明示的に条件付けており、
返信前の締結送信は相手方に押し付け感を与える。ただし開始日が本日で時間的余裕がないため、
待つのではなく短い催促を送って当日中の確認を促すのが最短ルート。
#12は業務委託契約で開始日が締結日と同日〜数日前後することは実務上一般的であり、
条件変更（＝相手方の再確認が必要になる）のコストの方が大きい。

### C. 情報補完で解決（要確認）

| 項目 | 必要な情報 | 補完者 |
|---|---|---|
| #7 | 菊池様の漢字フルネーム、法人格の有無、法人名 | 野村様 |
| #3 | 契約書PDFのDrive `03_契約管理` への保存 | 野村様（1操作） |
| #13 | GMOサイン管理画面での送信枠残の確認 | 野村様／松本様 |

---

## 送信可否の結論

**現時点で送信してはならない。**

最低限、以下3点の解消が送信の必要条件。

1. #7（相手方氏名の確定）— GMOサインの受信者設定に必須項目
2. #9（署名位置）— PDF実体を確認しないと署名欄を誤配置するリスクがある
3. #8（相手方確認の扱い）— 野村様の決裁

#3・#4・#5 は、野村様が添付PDFをDriveへ保存すれば本セッションから解消可能。
